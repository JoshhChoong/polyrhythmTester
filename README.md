if width > length:
   raise popup(if on mobile, please rotate screen for optimal experience)

DOC:
Gamemodes:
   HTML title text disapears

      Trainer mode (1):
         VisualDisplay(1)
      Test mode (2):
         VisualDisplay(2)
         10 second period, each chord(?) gets on second
         only tells results after completion
         gives accuracy score, su

      Gives Accuracy Statistic
         accuracy = Correct keys pressed / total keys pressed

Keybinds: 
   Customize keyboards, have two(?) presets

class VisualDisplay(x):
   (x) Each keybind has different colour associated, (yellow blue green indigo grey pink white)
   (x) Virtual keyboard (asdf jkl;) - corresponds to (1234 5678 rythem)
   (x=1) Visual trail of letters going down 
   (x=1) real time feedback if incorect letter is pressed (blue vs red)



